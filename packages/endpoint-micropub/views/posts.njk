{% extends "document.njk" %}

{% block content %}
  {%- if posts %}
  {{ fileList({
    items: posts
  }) }}
  {{ pagination(pages(page, limit, count)) }}
  {%- else -%}
  <p>{{ __("micropub.posts.none") }}<p>
  {%- endif %}
{% endblock %}
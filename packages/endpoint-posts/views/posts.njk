{% extends "document.njk" %}

{% block content %}
  {%- if posts.length > 0 %}
  {{ fileList({
    items: posts
  }) }}
  {{ pagination(pages(page, limit, count)) }}
  {%- else -%}
  {{ prose({ text: __("posts.posts.none") }) }}
  {%- endif %}
{% endblock %}
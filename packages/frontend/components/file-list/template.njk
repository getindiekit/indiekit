<ol class="file-list{{- " " + opts.classes if opts.classes }}">
  {%- for item in opts.items %}
  <li class="file-list__item">
    <h2 class="file__title">
      {{ icon(item.properties["post-type"]) }}<a href="{{ item._id | absoluteUrl(parentUrl) }}">{{ item.properties.name or item.properties["post-type"] | capitalize }}</a>
    </h2>
    <p class="file__meta">{{ item.properties.published | date("p • PPP") }}</p>
  </li>
  {%- endfor %}
</ol>
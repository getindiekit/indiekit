{%- set tags = opts.items if (opts.items | isArray) else [opts.items] -%}
<dl class="prose--caption">
  <dt class="-!-visually-hidden">{{ opts.label }}</dt>
  {% for tag in tags %}<dd class="tag">{{ tag }}</dd>{% endfor %}
</dl>
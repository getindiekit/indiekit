{%- if opts.text -%}
{%- set label -%}
<label class="{{ classes("label", opts.classes) }}"
  {%- if opts.for %} for="{{ opts.for }}"{% endif %}
  {{- attributes(opts.attributes) }}>
  {{- opts.text | safe -}}
</label>
{%- endset -%}
{{ label | safe }}
{%- endif -%}
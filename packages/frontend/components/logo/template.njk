<a class="{{ classes("logo", opts) }}" href="{{ opts.href }}">
  <img class="u-logo p-name" src="{{ opts.src }}" alt="{{ opts.alt }}" height="{{ opts.size | default(16) }}" width="{{ opts.size | default(16) }}">
</a>
{%- set legend %}
  {%- if opts.legend.text %}
  <legend class="{{ classes("fieldset__legend", opts.legend.classes) }}">
  {%- if opts.legend.isPageHeading %}
    <h1 class="fieldset__title">
      {{- opts.legend.text | safe -}}
    </h1>
  {%- else %}
    {{- opts.legend.text | safe -}}
  {%- endif %}
  </legend>
  {%- endif %}
{%- endset %}
<fieldset class="{{ classes("fieldset", opts) }} s-linear"
  {%- if opts.role %} role="{{ opts.role }}"{% endif %}
  {%- if opts.describedBy %} aria-describedby="{{ opts.describedBy }}"{% endif %}
  {{- attributes(opts.attributes) }}>
  {{- legend | safe }}
  {{- caller() if caller }}
</fieldset>
{% extends "document.njk" %}

{{ "APPLICATION" | debug }}
{{ application | debug }}

{{ "OPTIONS" | debug }}
{{ opts | debug }}

{% block header %}
  {{ header({
    url: application.url,
    name: application.name
  }) }}
{% endblock %}

{
  "token": {
    "title": "Token endpoint",
    "guidance": {
      "discovery": "To use this token endpoint, add the following value to your website’s `<head>`:",
      "verifyTitle": "Verify an access token",
      "verifyRequest": "To verify the access token, make a `GET` request to the token endpoint with the access token in the header:",
      "verifyResponse": "This endpoint will then verify the token and respond with information about the user and scope:"
    }
  }
}

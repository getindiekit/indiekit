{% extends "form.njk" %}

{% block form %}
  {{ heading({
    text: title,
    parent: parent
  }) }}

  {{ prose({ text: __("files.delete.note", publication.store.info.name) }) }}

  {{ button({
    classes: "button--warning",
    text: __("files.delete.submit")
  }) }}

  {{ prose({
    text: "[" + __("files.delete.cancel") + "](" + filesPath + ")"
  }) }}
{% endblock %}
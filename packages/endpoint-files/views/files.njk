{% extends "document.njk" %}

{% block content %}
  {%- if files.length > 0 %}
  {{ cardGrid({
    items: files
  }) }}
  {{ pagination(cursor) }}
  {%- else -%}
  {{ prose({ text: __("files.files.none") }) }}
  {%- endif %}
{% endblock %}
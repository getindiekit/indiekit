{% extends "document.njk" %}

{% block content %}
  {%- if files.length > 0 %}
  {{ cardGrid({
    items: files
  }) }}
  {{ pagination(pages(page, limit, count)) }}
  {%- else -%}
  <p>{{ __("files.files.none") }}<p>
  {%- endif %}
{% endblock %}
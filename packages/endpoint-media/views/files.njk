{% extends "document.njk" %}

{% block content %}
  {%- if files %}
  {{ fileGrid({
    items: files
  }) }}
  {{ pagination(pages) }}
  {%- else -%}
  <p>{{ __("media.files.none") }}<p>
  {%- endif %}
{% endblock %}
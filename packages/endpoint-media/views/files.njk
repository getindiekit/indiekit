{% extends "document.njk" %}

{% block content %}
  {%- if files %}
  {{ fileGrid({
    items: files
  }) }}
  {{ pagination(pages(page, limit, count)) }}
  {%- else -%}
  <p>{{ __("media.files.none") }}<p>
  {%- endif %}
{% endblock %}